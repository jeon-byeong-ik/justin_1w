# 작업 분해 (Tasks) — 도마뱀 발사!

> **단계 4: 무엇을, 어떤 순서로 만들 것인가.** 마일스톤 단위로 작게 쪼개 자주 검증한다(원칙 8).
> 각 작업은 가능하면 독립적으로 완료·플레이 검증 가능하도록 구성한다.

- **제작자:** justin
- **상태:** 초안 (구현 전)
- **작성일:** 2026-06-27
- **범례:** ☐ 미착수 / ◐ 진행중 / ☑ 완료 · `[P1]` 우선순위 · `(USx)` 관련 스토리

---

## M0 — 프로토타입: "발사가 된다" (핵심 손맛 검증)
> 목표: 못생겨도 좋으니 **충전→발사→비거리**가 손맛 있게 동작. 여기서 재미 없으면 방향 수정.

- ☐ `[P1]` T0.1 프로젝트 셋업 (Vite + TypeScript + Canvas, 빈 화면 렌더) *(plan §1)*
- ☐ `[P1]` T0.2 입력 추상화: 탭/스페이스 → `TAP`/`CONFIRM` 액션 *(US-6, plan §2.2)*
- ☐ `[P1]` T0.3 파워 게이지: 연타 충전 + 자연 감소 + 3초 타이머 *(US-1, GDD §4.1)*
- ☐ `[P1]` T0.4 발사 공식 1차 구현 `computeLaunch` (각도 고정 45°) *(contracts)*
- ☐ `[P1]` T0.5 비행 렌더: 포물선 궤적 + 도마뱀 스프라이트(임시) + 비거리 표시 *(US-1)*
- ☐ `[P1]` T0.6 상태머신: READY→CHARGE→FLIGHT→RESULT→다시하기 *(plan §2.1)*
- ☐ `[P2]` T0.7 **플레이 검증 회고**: 손맛 OK? 제한시간/증가량 1차 튜닝 *(GDD §9)*

**M0 완료 정의:** 키 하나로 충전→발사→비거리 확인→다시하기가 10초 내 반복된다.

---

## M1 — 코어 루프: 실력이 반영된다
> 목표: 각도/타이밍 도입, 결과 화면, 저장. "잘하면 더 멀리"가 체감된다.

- ☐ `[P1]` T1.1 각도 게이지: 왕복 바늘 + 스위트스팟 + 확정 입력 *(US-2, GDD §4.1)*
- ☐ `[P1]` T1.2 발사 공식에 `sweet`·`angle` 결합, 퍼펙트 판정 *(contracts §3)*
- ☐ `[P1]` T1.3 발사 공식 단위 테스트(불변식 A~F) *(contracts §4, Vitest)*
- ☐ `[P1]` T1.4 결과 화면: 비거리/퍼펙트/획득 에너지/신기록 *(US-4, GDD §7.2)*
- ☐ `[P1]` T1.5 저장: 최고기록·누적 에너지 localStorage *(US-4, plan §2.3)*
- ☐ `[P2]` T1.6 오버차지 구간 + 위험/보상 밸런싱 *(GDD §9)*
- ☐ `[P2]` T1.7 효과음 1차(연타·발사·퍼펙트·착지) *(US-8)*

**M1 완료 정의:** 같은 사람이 연습하면 기록이 늘고, 기록이 저장된다.

---

## M2 — 진화: 성장 보상
> 목표: 도마뱀→모토마→미라이돈 진화와 능력치 상승, 진화 연출.

- ☐ `[P1]` T2.1 진화 단계·능력치 테이블 `evolution.ts` *(GDD §3.3)*
- ☐ `[P1]` T2.2 누적 에너지 임계값 도달 시 진화 트리거 *(US-3, GDD §3.2)*
- ☐ `[P1]` T2.3 진화별 스프라이트 3종 + HUD 진화 표시(●●○) *(GDD §3.1, §7.1)*
- ☐ `[P1]` T2.4 진화 연출(암전→발광→변형→배너) + 스킵 *(GDD §3.4)*
- ☐ `[P2]` T2.5 능력치 변화가 공식에 반영됨을 테스트(불변식 D) *(contracts)*

**M2 완료 정의:** 반복 플레이로 진화하며, 진화 후 눈에 띄게 더 멀리 난다.

---

## M3 — 2인 대결: 같이 논다
> 목표: 로컬 턴제 대결(베스트 오브 3)과 리매치. 입력 분리.

- ☐ `[P1]` T3.1 세션/턴 관리 `session.ts` (P1→P2→비교) *(US-5, plan §2.1)*
- ☐ `[P1]` T3.2 입력 키 분리(P1 `A` / P2 `L`) + 화면 영역 분리(터치) *(US-5,6)*
- ☐ `[P1]` T3.3 대결 결과: 양측 비거리·승자·스코어(2선승) *(US-5, GDD §7.2)*
- ☐ `[P1]` T3.4 리매치 흐름(결과→즉시 재대결) *(US-5)*
- ☐ `[P2]` T3.5 핸디캡 옵션(진화 격차 보정) *(GDD §5.2)*

**M3 완료 정의:** 두 명이 같은 기기에서 충돌 없이 대결하고 즉시 리매치한다.

---

## M4 — 살붙이기: 콘텐츠 & 폴리시
> 목표: 스토리 지역, 상점, 스킨, 사운드/연출 완성도.

- ☐ `[P2]` T4.1 스토리 4개 지역 + 해금 + 바람/상승기류 *(GDD §2.3)*
- ☐ `[P2]` T4.2 상점: 부스터/영구강화/스킨 + 경제 밸런싱 *(US-7, GDD §6)*
- ☐ `[P2]` T4.3 무한 챌린지 + 로컬 리더보드 *(GDD §5.1)*
- ☐ `[P3]` T4.4 일일 도전(시드 고정) *(GDD §5.1, plan §2.4)*
- ☐ `[P3]` T4.5 배경 패럴랙스·진화 이펙트·BGM(오리지널 자산) *(GDD §8)*
- ☐ `[P3]` T4.6 접근성(색맹 무늬 표기, 언어 토글) *(GDD §7.3)*

**M4 완료 정의:** 처음~엔딩까지 플레이 가능하고, 꾸미기·도전 욕구가 생긴다.

---

## 작업 진행 규칙
1. **위에서 아래로.** M0이 검증되기 전에는 M1로 넘어가지 않는다(원칙 8).
2. **공식 변경은 문서부터.** 밸런싱 수치 변경은 `contracts` / `game-design.md` 갱신 후 코드 반영.
3. **순수 로직은 테스트 동반.** `core/*`는 Vitest 없이 머지하지 않는다(plan §2.3).
4. **플레이 검증.** 각 마일스톤 끝에 실제로 플레이하고 회고를 남긴다.

---

### 선결 과제 (구현 시작 전 justin 확정)
- [ ] 기술 스택 확정 (plan §6 T1) — *권장: Vite + TS + Canvas*
- [ ] 발사 메커니즘 확정 (spec §7 Q1) — *권장: 파워 연타 → 각도 타이밍 2단계*
- [ ] 2인 방식 확정 (spec §7 Q2) — *권장: v1 턴제*
- [ ] 진화 기준 확정 (spec §7 Q4) — *권장: 누적 재화*

이 4가지가 정해지면 **M0 T0.1부터 바로 구현 착수** 가능합니다.
